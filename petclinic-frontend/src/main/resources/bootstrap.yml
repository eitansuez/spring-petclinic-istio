spring:
  application:
    name: petclinic-frontend
  cloud:
    kubernetes:
      reload:
        strategy: restart-context
        enabled: true
      config:
        name: petclinic-config
        namespace: default
management:
  endpoint:
    restart:
      enabled: true
