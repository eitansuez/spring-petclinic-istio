spring:
  application:
    name: petclinic-frontend
  cloud:
    kubernetes:
      reload:
        strategy: restart-context
        enabled: true
      config:
        namespace: default
        sources:
        - name: petclinic-config
management:
  endpoint:
    restart:
      enabled: true
