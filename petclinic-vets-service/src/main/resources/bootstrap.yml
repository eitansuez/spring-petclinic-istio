spring:
  application:
    name: vets-service
  cloud:
    kubernetes:
      config:
        name: petclinic-config
        namespace: default
        sources:
        - name: petclinic-config
      reload:
        strategy: restart-context
        enabled: true
management:
  endpoint:
    restart:
      enabled: true