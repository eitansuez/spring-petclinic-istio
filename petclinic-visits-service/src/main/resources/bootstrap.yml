spring:
  application:
    name: visits-service
  cloud:
    kubernetes:
      reload:
        strategy: restart-context
        enabled: true
      config:
        name: petclinic-config
        namespace: default
management:
  endpoint:
    restart:
      enabled: true