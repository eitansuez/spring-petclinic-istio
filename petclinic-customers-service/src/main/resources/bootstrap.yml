spring:
  application:
    name: customers-service
  cloud:
    kubernetes:
      reload:
        strategy: restart-context
        enabled: true
      config:
        name: petclinic-config
        namespace: default
management:
  endpoint:
    restart:
      enabled: true